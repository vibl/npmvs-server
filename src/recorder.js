const compileData = require('./extractor/producer');

const record = async () => {
  // Get all dirty packages
  // Loop :
  //   - const data = compileData(packName)
  //   - await recordData(data)
};